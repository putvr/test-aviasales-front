import React from 'react';
import logo from './logo.svg';

const Header = () => {
    return (
        <header>
            <div>
                <img src={logo} alt=""/>
            </div>
        </header>
    );
};

export default Header;